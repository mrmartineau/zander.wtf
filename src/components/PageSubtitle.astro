<h2><slot /></h2>

<style>
  h2 {
    font-family: var(--font-body);
    font-style: italic;
    font-weight: 400;
    font-size: var(--step-1);
    line-height: 1.4;
    color: var(--color-muted);
    margin-block-start: var(--space-xs);
    text-wrap: balance;
  }
</style>
