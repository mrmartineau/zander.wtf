---
interface Props {
  date: Date;
}

const { date } = Astro.props;
const dateString = date.toLocaleDateString(navigator.language || 'en-GB', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
const timestamp = date.toISOString();
---

<time datetime={timestamp}>{dateString}</time>
