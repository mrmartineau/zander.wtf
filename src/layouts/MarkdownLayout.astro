---
import ContentLayout from './ContentLayout.astro';

const { title, subtitle, slug } = Astro.props.frontmatter;
---

<ContentLayout title={title} subtitle={subtitle} slug={slug}>
  <article class="flow">
    <slot />
  </article>
</ContentLayout>
