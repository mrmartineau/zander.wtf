<div class="zm-block zm-tv">
  <header>
    <h3 class="zm-block-title">Watching</h3>
  </header>
  <div class="zm-tv-list">
    {% for item in collections.tv %}
    <img src="{{ item.images.medium }}" class="zm-tv-item" />
    {% endfor %}
  </div>
</div>
